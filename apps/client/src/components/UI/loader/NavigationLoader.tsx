/**
 * NavigationLoader intentionally disabled.
 * The previous implementation forced a full-screen overlay on every route change,
 * which made navigation feel slow and caused the navbar to appear stuck.
 */
export default function NavigationLoader() {
  return null
}
